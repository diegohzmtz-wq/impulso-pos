"use client";

import ProtegerRuta from "@/components/ProtegerRuta";

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <ProtegerRuta rolesPermitidos={["Admin", "Gerente"]}>
      {children}
    </ProtegerRuta>
  );
}